# MikanEngine 开发路线图（2026 H2）

> 基线日期：2026-08-21  
> 适用工程：`vulkan engine`（C++17 + Vulkan 主工程）  
> 核心原则：先可靠、再生产、后扩展。未来一段时间不以增加渲染特性数量为主要目标。

## 1. 当前定位

MikanEngine 已经越过渲染 Demo 阶段，具备运行时、编辑器、项目管理、ECS、场景序列化、游戏插件、2D/3D 物理、动画、音频、预制体、后处理与 headless 验证等完整链路。

当前主要问题不是功能不足，而是：

1. Git 基线已建立并推送远程；仍需补充稳定版本标签和自动化回归。
2. 有 headless、自测和场景校验入口，但没有统一、可重复的回归套件。
3. 资产加载、缓存、释放和热更新尚未形成统一生命周期。
4. 渲染模块功能密集，部分巨型文件和隐式状态增加回归风险。
5. 文档、计划和实际实现状态存在漂移。
6. 尚未用一个完整小游戏验证“创建项目到发布包”的生产流程。

## 2. 总体目标

本路线图结束时，应达到以下状态：

- 每次修改都有版本记录，可以快速回退和定位回归。
- 一条命令能够完成构建、场景校验和核心回归测试。
- 至少一个小型游戏可以从独立项目目录构建成可分发包。
- 场景和项目数据具备显式版本与迁移机制。
- 资产拥有统一句柄、状态、缓存、释放和重新加载流程。
- Vulkan Validation 在标准回归场景中无 error。
- 渲染 pass 的输入、输出和同步关系可以被明确检查。
- 新增功能不再依赖扩大核心巨型文件。

## 3. 优先级规则

按以下顺序决定是否开发：

1. 会造成代码或项目数据丢失的问题。
2. 会导致已有场景、编辑器或游戏回归的问题。
3. 阻塞完整游戏制作与发布的问题。
4. 明显影响迭代效率和性能的问题。
5. 新功能和画面效果。

除非由实际游戏需求阻塞，以下内容暂缓：网络、第二套脚本语言、全新渲染后端、复杂粒子系统、更多抗锯齿算法、更多实验性 GI 和大规模编辑器换肤。

---

## Phase 0：建立安全基线（P0，2～3 天）

### 目标

结束依靠整目录备份管理版本的状态，建立唯一可信主线。

### 工作项

- [x] 在 `vulkan engine` 根目录初始化 Git并推送首个基线提交。
- [x] 完善 `.gitignore`：排除 `out/`、`.vs/`、日志、缓存、临时 DLL、崩溃文件和本地备份。
- [ ] 对第三方依赖、引擎资产和大型测试素材制定跟踪策略；必要时使用 Git LFS。
- [ ] 创建首个可构建基线标签，例如 `baseline-2026-08`。
- [ ] 将现有完整目录备份移出活跃源码树；确认 Git 基线可靠后再决定是否归档或删除。
- [x] 修正文档中的旧路径和已过期状态，但不在本阶段重写全部文档。
- [ ] 记录编译器、Windows SDK、Vulkan SDK、CMake、Ninja 和关键 DLL 版本。

### 验收

- 全新工作目录能够从版本库还原源码。
- `tools/build.ps1` 能完成 Release 构建。
- 修改一个文件后可准确查看差异并恢复。
- 稳定基线有明确标签和构建说明。

### 阶段门槛

Phase 0 未完成前，不进行跨文件大重构。

---

## Phase 1：自动化回归闭环（P0，1～2 周）

### 目标

让“能启动”升级为“核心行为可自动验证”。

### 工作项

- [ ] 新建统一入口 `tools/test.ps1`，依次执行构建、场景校验、headless 运行和断言。
- [ ] 测试脚本返回稳定退出码，并将日志、dump 和结果汇总到独立测试输出目录。
- [ ] 建立最小回归矩阵：
  - [ ] 空场景启动与退出。
  - [ ] 3D 模型、材质、相机和方向光。
  - [ ] 骨骼动画时间推进。
  - [ ] 2D Sprite、文字、Canvas 和显隐切换。
  - [ ] Box2D 重力、碰撞和触发器。
  - [ ] Jolt 刚体落地。
  - [ ] 场景 2D → 3D → 2D 切换。
  - [ ] 游戏插件加载和至少一次热重载。
  - [ ] 预制体保存、实例化和父子层级保持。
  - [ ] 场景保存后重新加载的一致性。
- [ ] 为 dump 增加稳定字段，避免测试依赖日志文本。
- [ ] 把现有 `--phys2d-selftest`、`--prefab-selftest` 纳入统一测试入口。
- [ ] 加入 Vulkan Validation 日志扫描：标准回归中出现 error 即失败。
- [ ] 将测试接入 CTest，或明确由 PowerShell 作为唯一测试调度器；避免维护两套不一致入口。

### 验收

- 一条命令可在无人值守状态下执行全部核心回归。
- 连续运行三次结果一致。
- 任意场景加载失败、崩溃、Validation error 或状态断言失败都会产生非零退出码。
- 核心测试总耗时控制在 5 分钟内，另设较慢的完整测试组。

---

## Phase 2：做完一个可发布游戏（P0，2～3 周）

### 目标

用真实内容生产验证引擎，而不是继续用孤立功能证明引擎。

### 推荐项目

选择 `breakout` 或 `snake` 作为首个发布对象。优先推荐 `breakout`：它能覆盖输入、2D 物理、UI、音频、关卡状态、暂停、胜负流程和存档，但规模可控。

### 工作项

- [ ] 将游戏整理为独立 `projects/<name>/` 项目结构。
- [ ] 完成主菜单、游戏、暂停、失败、胜利和重新开始流程。
- [ ] 完成基础音效、背景音乐、分辨率适配和输入映射。
- [ ] 验证保存设置或最小进度数据。
- [ ] 补齐缺失资源提示，不允许静默黑屏或无响应。
- [ ] 完善 `tools/publish.ps1`：只复制运行必需文件，生成版本信息和发布清单。
- [ ] 在一个不含源码和开发环境变量的干净目录中启动发布包。
- [ ] 将该游戏的关键流程加入 Phase 1 回归套件。

### 验收

- 用户解压发布包后可直接启动。
- 从主菜单进入游戏、完成一局、重新开始并退出，全流程无崩溃。
- 发布目录不依赖源码路径、构建目录或绝对路径。
- 缺少资源时给出明确错误和非零测试结果。

### 阶段门槛

首个游戏未发布前，不新增大型引擎子系统。

---

## Phase 3：场景与项目数据可靠性（P1，1～2 周）

### 目标

保证今天保存的项目在未来版本仍然可诊断、可迁移。

### 工作项

- [ ] 为场景、预制体和 `project.json` 增加 `formatVersion`。
- [ ] 建立逐版本迁移函数，禁止在反序列化代码中散布匿名兼容分支。
- [ ] 未知组件、未知字段、缺失必需字段应生成结构化诊断；编辑器可选择继续打开只读或中止。
- [ ] 保存采用临时文件 + 原子替换，避免写入中断损坏原文件。
- [ ] 保存前保留有限数量的自动恢复版本。
- [ ] 建立场景往返测试：加载 → 保存 → 重载后，对规范化 JSON 或 ECS 状态进行比较。
- [ ] 为实体 ID、层级循环、悬空引用和重复 ID 增加校验。
- [ ] Undo/Redo 从全场景快照逐步迁移为命令或差量操作；先覆盖 Transform、组件字段、创建/删除实体和层级调整。

### 验收

- 至少两个旧版本测试场景能自动迁移到当前格式。
- 强制中断模拟不会破坏最后一次有效场景。
- 100 次常用编辑操作的 Undo/Redo 不产生状态漂移。

---

## Phase 4：统一资产生命周期（P1，3～4 周）

### 目标

解决重复加载、所有权不明、热更新分散以及大场景启动阻塞问题。

### 设计边界

- 资产引用使用稳定 `AssetId` 或句柄，不让业务代码长期持有裸 Vulkan 资源。
- CPU 解码、GPU 上传和资源切换分为三个明确阶段。
- 先支持现有高频资产，不一次性重写全部加载器。

### 工作项

- [ ] 建立 `AssetRegistry`：规范化路径、类型、状态、引用数、错误信息和版本。
- [ ] 首批接入 Texture、Model、Audio、Shader。
- [ ] 建立 `NotLoaded / Loading / Ready / Failed / Reloading` 状态机。
- [ ] CPU 文件读取和解码进入后台任务；GPU 创建和上传回到受控渲染线程阶段。
- [ ] 统一默认资产：白纹理、法线纹理、错误材质和占位模型。
- [ ] 编辑器资产窗口显示状态、引用数、内存估算和错误，并提供重新加载。
- [ ] 文件变更触发资产级热更新；Shader 现有热更新迁移到统一事件入口。
- [ ] 定义场景切换、项目关闭和引擎退出时的释放顺序。
- [ ] 增加资产压力测试：重复打开场景、重复加载同一路径、热更新和项目切换。

### 验收

- 相同资产路径不会产生重复 GPU 实例。
- 后台加载期间主循环持续响应。
- 项目切换十次后资源数量和显存占用保持稳定区间。
- 资产失败可重试，且不会留下半初始化对象。

---

## Phase 5：渲染架构收敛与同步正确性（P1，3～5 周）

### 目标

在保留现有效果的前提下，降低新增 pass 和多视口渲染的风险。

### 工作项

- [ ] 先绘制并固化当前帧流程图：Scene View、Game View、Swapchain、阴影、GBuffer/Forward、后处理和 UI。
- [ ] 为每个 pass 声明输入、输出、尺寸、格式、访问类型和生命周期。
- [ ] 引入轻量 Render Graph 或 Pass Scheduler；第一阶段只管理依赖与 barrier，不追求完全自动化资源分配。
- [ ] 将临时 attachment 纳入统一池，按尺寸和格式复用。
- [ ] 消除 Scene View、Game View 与 Swapchain 之间的隐式共享状态。
- [ ] 将 compute pipeline 纳入统一 Shader 热更新。
- [ ] 梳理 descriptor、pipeline layout 和 push constant 的版本兼容检查。
- [ ] 拆分巨型渲染文件：按资源管理、剔除、命令录制、缓存序列化和调试功能划分。
- [ ] 建立 GPU 时间戳统计，至少覆盖阴影、主场景、后处理、UI 和体素路径。
- [ ] 在 NVIDIA、AMD 或集显中的至少两类 GPU 上跑核心回归；若设备不足，保留待验证清单。

### 验收

- 标准场景运行期间 Vulkan Validation 无 error。
- Scene View 与 Game View 同帧渲染互不污染。
- 连续 resize、切换项目、开关编辑器窗口不崩溃、不泄漏。
- GPU 性能面板能指出主要 pass 耗时。
- 新增简单后处理 pass 不需要修改多个不相关渲染器。

---

## Phase 6：性能、内存与大场景（P2，2～4 周）

### 目标

优化由数据证明的瓶颈，不进行无基准的重写。

### 工作项

- [ ] 建立三档固定基准场景：小型功能场景、中型游戏场景、大型压力场景。
- [ ] 记录 CPU 帧时间、GPU 帧时间、draw/dispatch 数、三角形数、显存估算、资产加载时间。
- [ ] 为启动、项目打开、场景切换和首帧分别计时。
- [ ] 依据数据优化剔除、批处理、纹理描述符、动画更新、体素上传和编辑器预览。
- [ ] 为缓存文件增加统一 magic、版本、校验和与失效策略。
- [ ] 检查线程安全、资源销毁延迟和设备丢失后的错误路径。

### 验收

- 每项性能优化都有优化前后的同场景数据。
- 连续加载压力场景不会出现持续内存增长。
- 性能退化超过约定阈值时，测试能够报警。

---

## Phase 7：按游戏需求扩展功能（P3，持续）

只有前面阶段稳定，且真实项目提出需求时才进入本阶段。

候选方向按推荐顺序排列：

1. 动画状态机、混合树与 Root Motion。
2. 粒子系统与可编辑特效资源。
3. 更完整的材质实例和 Shader Variant 管理。
4. 2D Tilemap 工作流、导航和关卡工具。
5. 体素内容生产和流式世界工具。
6. Lua/C# 等第二脚本方案。
7. 网络。

每项功能必须先回答：由哪个游戏需求驱动、如何测试、如何序列化、如何在编辑器中制作、失败时如何降级。

## 4. 建议时间表

| 时间 | 重点 | 预期结果 |
|---|---|---|
| 第 1 周 | Phase 0 + Phase 1 起步 | Git 基线、统一测试入口 |
| 第 2～3 周 | Phase 1 | 核心回归矩阵稳定 |
| 第 4～6 周 | Phase 2 | 第一个可分发小游戏 |
| 第 7～8 周 | Phase 3 | 场景版本、迁移和可靠保存 |
| 第 9～12 周 | Phase 4 | 核心资产进入统一生命周期 |
| 第 13～17 周 | Phase 5 | 渲染 pass 和同步关系收敛 |
| 第 18～20 周 | Phase 6 | 基准驱动的性能与内存治理 |

这是单人开发的参考节奏。若某阶段超期，应缩小交付范围，不应同时开启更多阶段。

## 5. 每周执行方式

每周只维护三类事项：

- 一个阶段性交付目标。
- 最多三个必须完成的任务。
- 一个可选任务。

建议循环：

1. 周初选择能产生可验收结果的最小切片。
2. 先补测试或复现，再改实现。
3. 每个切片单独提交，禁止把无关格式化和功能修改混在一起。
4. 周末运行完整回归，记录构建时间、失败项、遗留风险和下一步。
5. 未完成事项重新评估，不自动滚入下一周造成无限积压。

## 6. 完成定义（Definition of Done）

一个引擎任务只有同时满足以下条件才算完成：

- 代码能够使用标准脚本构建。
- 有自动测试，或明确记录无法自动化的人工验证步骤。
- Vulkan 相关修改检查过 Validation 输出。
- 新增数据有序列化、版本和错误处理策略。
- 新增资源有创建、重新加载和释放路径。
- 文档和 Schema 已同步。
- 没有依赖本机绝对路径或未记录环境状态。
- Git 中为独立、可理解、可回退的提交。

## 7. 当前立即执行的前三项

1. 完成 Phase 0：Git、安全基线和依赖版本清单。
2. 创建 `tools/test.ps1`，先接入空场景、baka3d、phys2d 和 prefab 四条测试。
3. 冻结新渲染特性，选择 breakout 作为第一个完整发布游戏。
