{
  "schemaVersion": 1,
  "name": "cesiumwalk-renderdoc-capture",
  "description": "验证场景后构建 MikanEngine，并用桌面 RenderDoc 抓取第 60 个 Present。",
  "allowDestructive": false,
  "defaults": {
    "timeoutSeconds": 300
  },
  "steps": [
    {
      "id": "validate_scene",
      "action": "validate_scene",
      "args": {
        "projectPath": "projects/third-person-navigation",
        "scenePath": "scenes/main.json",
        "checkAssets": true
      }
    },
    {
      "id": "build_engine",
      "action": "build",
      "dependsOn": ["validate_scene"],
      "args": {
        "target": "MikanEngine",
        "configureIfMissing": true,
        "killEngine": true
      }
    },
    {
      "id": "capture_frame",
      "action": "capture_frame",
      "dependsOn": ["build_engine"],
      "args": {
        "projectPath": "projects/third-person-navigation",
        "scene": "scenes/main.json",
        "game": "cesiumwalk",
        "captureFrame": 60,
        "frames": 90,
        "fixedDeltaSeconds": 0.016666667,
        "captureWaitSeconds": 10
      }
    }
  ]
}
