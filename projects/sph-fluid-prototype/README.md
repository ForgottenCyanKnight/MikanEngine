# SPH 粒子流体模拟原型

这是 MikanEngine 内部面试演示用的流体原型：

- CPU 端实现三维 SPH 密度、压力、粘性和边界积分；
- 通过引擎统一 Vulkan 粒子 Billboard 管线显示粒子；
- 固定时间步，支持暂停、重置；
- 后续可将求解器替换为 Vulkan Compute，并继续接入深度/厚度屏幕空间流体渲染。

运行时按 `Space` 暂停/继续，按 `R` 重置粒子。
