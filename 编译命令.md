# MikanEngine 编译命令速查

> 项目根:`D:\Engine project\vulkan engine`(Vulkan + SDL3 自研引擎)
> 构建系统:CMake + Ninja + MSVC(Release),产物在 `out\build\x64-Release\`

---

## ✅ 推荐:一键构建 `tools\build.ps1`(AI 与人都用这个)

> 封装了 VsDevCmd 引导 / 文件锁检测 / 日志重定向 / 错误摘要,退出码语义化(0=成功,1=失败,2=引擎在运行,3=环境错误)。
> VsDevCmd 路径由 vswhere 自动探测,不硬编码 VS 安装位置。

```powershell
# 构建 EngineMain(自动连带 Editor.dll / Game.dll / Shaders;无改动时秒级返回)
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1"

# 只构建 Editor.dll(改 PropertiesWindow / GameViewWindow 等编辑器代码)
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -Target Editor

# 只构建 Game.dll / 只重编 shader
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -Target Game
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -Target CompileShaders

# 构建 CPU-only 玩法测试宿主（连带 Game.dll，不需要 Editor.dll）
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -Target MikanTestRunner

# ⚠️ 引擎正在运行时(锁 Game.dll/Editor.dll → LNK1104):脚本默认拒绝构建(exit=2),加 -KillEngine 自动关引擎再构建
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -KillEngine

# out 被删除后自动配置；怀疑中间产物陈旧时 clean-first
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\build.ps1" -ConfigureIfMissing -CleanFirst
```

- 构建日志写入 `out\build\build.log`,失败时脚本自动打印前 20 条 `error C/LNK/FAILED` 摘要(不倾倒噪音)
- 自定义日志路径:`-LogPath <file>`

## 基础构建(最常用,等价于 build.ps1 内部)

```powershell
cmd /c "`"D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat`" -arch=x64 -host_arch=x64 >nul 2>&1 && cmake --build `"D:\Engine project\vulkan engine\out\build\x64-Release`" --target EngineMain"
```

## 构建 Editor 目标(编辑器改动必须)

> ⚠️ 手动 `cmake --build` 时,`EngineMain` 目标会**连带构建 Editor.dll**(ninja order-only 依赖,见 build.ninja),无需单独 `--target Editor`;只有只想重编 Editor 时才显式指定。

```powershell
cmd /c "`"D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat`" -arch=x64 -host_arch=x64 >nul 2>&1 && cmake --build `"D:\Engine project\vulkan engine\out\build\x64-Release`" --target Editor"
```

## 带日志重定向(推荐,便于过滤错误)

```powershell
cmd /c "`"D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat`" -arch=x64 -host_arch=x64 >nul 2>&1 && cmake --build `"D:\Engine project\vulkan engine\out\build\x64-Release`" --target EngineMain > `"D:\Engine project\vulkan engine\out\build\build.log`" 2>&1"

# 查看错误(只有真实错误,过滤 include 噪音):
Select-String -Path "D:\Engine project\vulkan engine\out\build\build.log" -Pattern "FAILED|error C|LNK"
```

## 干净全量构建(删除 out/ 之后)

> ⚠️ 空 out 目录直接 `cmake --build` 会报 `could not load cache`,必须**先配置**:

```powershell
# 1. 配置
cmd /c "`"D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat`" -arch=x64 -host_arch=x64 >nul 2>&1 && cmake -S `"D:\Engine project\vulkan engine`" -B `"D:\Engine project\vulkan engine\out\build\x64-Release`" -G Ninja -DCMAKE_BUILD_TYPE=Release -DCMAKE_C_COMPILER=cl -DCMAKE_CXX_COMPILER=cl"

# 2. 构建
cmd /c "`"D:\Program Files\Microsoft Visual Studio\2022\Community\Common7\Tools\VsDevCmd.bat`" -arch=x64 -host_arch=x64 >nul 2>&1 && cmake --build `"D:\Engine project\vulkan engine\out\build\x64-Release`" --target EngineMain"
```

## 运行与测试

```powershell
# 运行引擎:
Start-Process -FilePath "D:\Engine project\vulkan engine\out\build\x64-Release\EngineMain.exe" -WorkingDirectory "D:\Engine project\vulkan engine\out\build\x64-Release"

# 带参数(关闭体素世界,纯 2D 环境):
Start-Process -FilePath "D:\Engine project\vulkan engine\out\build\x64-Release\EngineMain.exe" -ArgumentList "--no-voxel-world"

# 捕获控制台输出(诊断日志, stderr 无缓冲):
Start-Process -FilePath "D:\Engine project\vulkan engine\out\build\x64-Release\EngineMain.exe" -RedirectStandardError "D:\Engine project\vulkan engine\out\build\err.txt"

# 等引擎退出再构建(避免文件锁):
Get-Process -Name "EngineMain" -ErrorAction SilentlyContinue | Wait-Process
```

## 场景校验(离线,写场景后立即纠错,无需启动引擎)

```powershell
# 校验场景 JSON（组件键/字段白名单、id 唯一、parent 引用、transform 结构）
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\validate_scene.ps1" "D:\Engine project\vulkan engine\assets\contact2d.json"

# 附带资源引用存在性检查（默认关）
powershell -NoProfile -ExecutionPolicy Bypass -File "D:\Engine project\vulkan engine\tools\validate_scene.ps1" my_scene.json -CheckAssets
```

> schema 来源:`tools\scene_schema.json`(引擎从 ComponentRegistry 反射表导出,**改组件字段后重新生成**):
> `EngineMain.exe --dump-schema "D:\Engine project\vulkan engine\tools\scene_schema.json"`

## 自动测试（推荐：玩法层 / 渲染层分离）

> `MikanTestRunner.exe`：玩法层 CPU-only 路径，不创建窗口、不初始化 SDL Video/Vulkan。
> `EngineMain.exe --headless`：渲染层路径，执行完整 SDL Video/Vulkan/FrameRender/Present。
> 两者的 `--dump-state <path>` 共用状态结构，`runtime_layer` 可判定实际运行层。
> 示例: contact2d 的 Box 从 y=360 落到 y=263.155(60 帧),AI 可用 `wpos[1]` 断言物理行为。

```powershell
# 玩法层跑 60 帧 + 导出状态 JSON
$root = "D:\Engine project\vulkan engine"
$exe = "$root\out\build\x64-Release\MikanTestRunner.exe"
$wd  = "D:\Engine project\vulkan engine\out\build\x64-Release"
$p = Start-Process -FilePath $exe -WorkingDirectory $wd -ArgumentList `
    "--frames","60","--fixed-dt","0.016666667","--dump-state","test_dump.json", `
    "--scene","$root\assets\contact2d.json","--game","contact2d" `
    -PassThru -Wait
Write-Host "exit=$($p.ExitCode)  (0=正常跑完; 崩溃看 log/crash_log.txt)"
Get-Content "$wd\test_dump.json"   # 实体坐标/可见性,断言用
```

## 自动测试(旧法:启动 + 检查崩溃)

```powershell
$root = "D:\Engine project\vulkan engine"
$p2 = Start-Process -FilePath "$root\out\build\x64-Release\EngineMain.exe" -WorkingDirectory "$root\out\build\x64-Release" -PassThru
Start-Sleep -Seconds 8
if (-not $p2.HasExited) { Write-Host "进程存活(未崩溃)"; Stop-Process -Id $p2.Id -Force }
Write-Host "crash_log 存在: $(Test-Path "$root\out\build\x64-Release\log\crash_log.txt")"
```

## MCP server（AI 开发-测试闭环,推荐 AI 客户端接入）

> 把 build / validate_scene / run_gameplay_test / run_render_test / read_dump / assert_state / engine_status / stop_engine 封装成 MCP 工具，玩法问题不再为测试而初始化 Vulkan。

**MCP client 配置（.mcp.json / 客户端设置）**:

```json
{
  "mcpServers": {
    "mikanengine": {
      "command": "powershell",
      "args": ["-NoProfile", "-ExecutionPolicy", "Bypass", "-File", "D:\\Engine project\\vulkan engine\\tools\\mcp_server.ps1"]
    }
  }
}
```

**工具速览**:

| 工具 | 作用 | 关键参数 |
|---|---|---|
| `build` | 一键构建引擎 | target, killEngine, cleanFirst, configureIfMissing |
| `validate_scene` | 离线校验场景 JSON | scenePath, checkAssets |
| `run_gameplay_test` | MikanTestRunner CPU-only 玩法测试 | scene, game, frames, fixedDeltaSeconds, extraArgs, timeoutMs |
| `run_render_test` | EngineMain 完整 Vulkan 渲染测试 | scene, game, frames, fixedDeltaSeconds, extraArgs, timeoutMs |
| `run_test` | 兼容入口（render=false 玩法 / true 渲染） | 同上，另含 render |
| `read_dump` | 读 run_test 的状态 JSON | path（可省略） |
| `assert_state` | 机器判定实体坐标/旋转/缩放/可见性 | path（可省略）, assertions[] |
| `engine_status` / `stop_engine` | 进程与崩溃状态 / 杀引擎释放文件锁 | — |

> 典型 AI 流程：`validate_scene` → `build(target=MikanTestRunner)` → `run_gameplay_test` → `assert_state`；只有 shader/GPU/交换链问题才走 `build(target=EngineMain)` → `run_render_test`。
> 手工验证 server: `echo '{"jsonrpc":"2.0","id":1,"method":"initialize","params":{}}' | powershell -NoProfile -File tools\mcp_server.ps1`

---

## ⚠️ 关键陷阱

| 问题 | 处理 |
|---|---|
| **LNK1104 无法打开 Game.dll / Editor.dll** | 引擎正在运行锁文件 → 用 `tools\build.ps1 -KillEngine` 自动关引擎再构建(手动则先关引擎或 `Get-Process EngineMain \| Wait-Process` 等退出) |
| **改 Editor 代码但只构建 EngineMain** | 会连带构建 Editor.dll(ninja order-only 依赖),能生效;只想重编 Editor 用 `-Target Editor` |
| **空 out 目录直接 `cmake --build`** | 报 `could not load cache` → 用 `tools/build.ps1 -ConfigureIfMissing`；陈旧产物用 `-CleanFirst` |
| **产物路径** | 在 `out\build\x64-Release\` 顶层(旧嵌套 `x64-Release\x64-Release\` 已清理) |
| **崩溃排查** | 默认看 exe 工作目录 `log/crash_log.txt`；MCP 看对应 `mcp_runs/<run-id>/crash_log.txt`；编译错误看 build.log |
| **改头文件(如 ECS/Components.h)** | 触发大范围重编,构建时间变长,属正常 |
