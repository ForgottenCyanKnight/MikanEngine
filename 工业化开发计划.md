# 工业化开发计划(MikanEngine Vulkan)

> 用途:把 MikanEngine 从"功能原型"推进到"半工业化引擎"的路线图。
> 依据:`D:\Engine project\vulkan engine` 代码现状审计(单线程渲染、无资产系统、无测试/CI、手写 JSON 序列化)。
> 优先级排序原则:**先打地基(版本控制+回归手段)→ 再动核心架构(资产/渲染)→ 再做数据可靠性 → 最后做迭代体验**。不做地基就动大架构,等于裸奔。

---

## 阶段总览

| 阶段 | 主题 | 工作量(单人全职) | 里程碑 |
|---|---|---|---|
| Phase 0 | 工程地基(git / 清理 / 日志 / 测试框架 / CI) | 1-2 周 | 有版本控制 + 回归手段,后续大改不裸奔 |
| Phase 1 | 资产系统与异步加载 | 3-4 周 | 启动不再卡顿,资产有统一生命周期 |
| Phase 2 | 渲染并行化与同步正确性 | 3-5 周 | Validation 零报错,帧流水化 |
| Phase 3 | 场景数据可靠性(版本化 + undo) | 2-3 周 | 存档向前兼容,编辑器可撤销 |
| Phase 4 | 迭代回路(shader 热重载 / 管线缓存 / GPU profiler) | 2 周 | 改 shader 保存即生效 |
| Phase 5 | 质量闭环(测试覆盖 / 崩溃增强 / 统计面板 / CI 扩展) | 1-2 周 | 每次提交可全量回归 |
| Phase 6 | 功能面(脚本 / 动画 / 后处理栈 / 2D 物理 / 网络) | 每项 2-6 周 | 按游戏需求独立立项 |

**总计:到 Phase 5 约 12-18 周**,产物是"半工业"引擎(资产+并行渲染+可靠数据+快速迭代),足以支撑内容团队做 demo。

---

## Phase 0 — 工程地基(先决条件,1-2 周)

> 为什么最先:后面每个阶段都要大改代码,没有版本控制、回归测试和可读日志,任何改动都是赌博。

### 0.1 git 化
- [ ] 初始化 git 仓库;写 `.gitignore`:排除 `out/`、`android/.gradle/`、`android/app/.cxx/`、`.vs/`、`.trae/`、`*.bak`、`lib/x64/*.dll`、`dll/`、`engine/shaders/spv/`(产物可再生成)、`imgui.ini`、`crash_log.txt`、`build*.log`
- [ ] 关键决策:`.bak` 文件(~15 个,见 `src/` 下 `*.bak`)逐一 `fc`/diff 确认与现文件一致后**删除**,历史由 git 承担

### 0.2 日志系统(替换 printf)
- [ ] `include/Core/Log.h` + `src/Core/Log.cpp`:分级(Info/Warn/Error/Debug)、时间戳、可选写 `engine.log`(滚动)、`__ANDROID__` 走 logcat 后端
- [ ] 全局替换 `LOGI/LOGD/LOGE` 宏到 Log;启动/帧计时/崩溃信息全部走日志
- [ ] 验收:`--no-voxel-world` 启动,`engine.log` 有分级时间戳输出

### 0.3 测试框架
- [ ] 引入 Catch2(v2,单头文件,契合当前"无包管理器"现状;后续可换 doctest)
- [ ] 建 `tests/` CMake target(纯逻辑模块先行,不碰 Vulkan):
  - `SceneSerializer` 序列化往返(现有 `sence.json` 作黄金样本)
  - `Chunk` 网格生成(固定种子 → 面数/顶点数断言)
  - `AABB` / `QuadTreeCulling` / `ModelBVH` 剔除查询
  - 2D `Canvas2D` 层级位置累加
- [ ] 验收:`ctest` 全绿

### 0.4 CI(可选但推荐)
- [ ] GitHub Actions:Windows + VsDevCmd 构建 EngineMain → 启动 8 秒冒烟(复用 `编译命令.md` 的自动测试脚本)→ 跑 ctest

---

## Phase 1 — 资产系统与异步加载(3-4 周)

> 最高业务价值:消灭启动卡顿、统一资产生命周期、给"改名断链"一个明确报错流。
> 与 Phase 2 的交汇点:"上传"——本阶段先定 AssetManager 接口,上传队列实现在 Phase 2 落地。

### 1.1 AssetManager 核心
- [ ] `include/Core/AssetManager.h` + `src/Core/AssetManager.cpp`:
  - `AssetId`(内容 hash,64 位)+ `AssetType`(Model/Texture/Font/Vox/Audio)
  - 注册表:路径 → AssetId → 资产槽(加载状态机 `NotLoaded/Loading/Ready/Failed`)
  - 引用计数 + 生命周期(编辑器资源窗口"卸载未使用资产")
  - 缺失资产 → 占位对象(粉色纹理 / 线框网格)+ 明确日志,不崩溃
- [ ] 编辑器 `AssetsWindow` 扩展:资产列表(状态/引用数/内存)、右键"重新加载""显示缺失"

### 1.2 各加载器接入
- [ ] `ModelRenderer`(`SceneRenderer::GetModelRenderer` 的路径 map)→ AssetManager
- [ ] `TexturePool`(贴图/立方体贴图)→ AssetManager
- [ ] `FontAtlas`(字型槽属运行期动态,资产层只管 TTF 源文件)
- [ ] `VoxLoader` / `AudioManager` → AssetManager
- [ ] 序列化引用从路径字符串改为 AssetId,存路径做显示名(**决策:磁盘仍存路径,加载时解析注册**,保持场景文件可读,不引入 GUID 迁移成本)

### 1.3 异步加载
- [ ] 后台线程队列(加载回调在主线程轮询完成)
- [ ] 上传放独立 transfer 队列(接口先占位,Phase 2 实现)
- [ ] 验收:大场景(如 Sponza 多副本)启动无卡帧;同资产只加载一次(日志去重)

---

## Phase 2 — 渲染并行化与同步正确性(3-5 周)

> 现状:全部 `vkQueueSubmit` 走唯一 `g_Queue`(`VulkanManager.cpp:231`),大量上传裸提交 `VK_NULL_HANDLE` fence,同步靠"单队列顺序"侥幸正确。目标:确定性同步 + 帧流水。

### 2.1 队列与同步
- [ ] 独立 transfer 队列(上传/拷贝走它,图形队列只渲染);设备无 transfer 队列时 fallback 共享队列
- [ ] 全部上传提交补 fence/信号量:提交后立即 fence 等待太慢的,改 per-frame 回收(见 2.2)
- [ ] 验收:开启 Vulkan Validation layers 跑完整 demo,**零报错**(现状必有大量 sync 警告)

### 2.2 Frame-in-flight(帧流水)
- [ ] 2-3 帧并行:per-frame 命令池/命令缓冲/descriptor 池/上传缓冲,逻辑帧 n 与渲染帧 n-1 重叠
- [ ] 现有全局命令池 `g_CommandPool` 拆 per-frame
- [ ] 主循环重构(`EngineMain.cpp:439`):`Update() → (后台)Record() → Submit+Present` 三步解耦
- [ ] 验收:同场景帧时间下降(CPU 提交与 GPU 执行重叠),无明显内存增长

### 2.3 (可选)multithreaded command recording
- [ ] 按 pass 分线程记录(`PrepareFrame`/`RenderGeometryOpaque`/debug overlay 各自线程),主线程提交
- [ ] 验收:多线程记录开启后帧时间不劣化,Validation 无并发报错

### 2.4 管线缓存
- [ ] `VkPipelineCache` 持久化到 exe 旁(与 imgui.ini 同策略,`SDL_GetBasePath`)
- [ ] 验收:二次启动管线创建耗时显著下降

---

## Phase 3 — 场景数据可靠性(2-3 周)

> 现状:`SceneSerializer.cpp`(79KB)手写 JSON 解析,无版本号、无 schema、无迁移——格式一变,老存档全废。这是工业引擎最不能接受的。

### 3.1 版本化 + 迁移
- [ ] 场景文件加 `"version": N`;加载时按版本逐级升级(迁移函数链)
- [ ] 加载器对未知键**警告而非忽略**(现在静默),对缺必需键报错
- [ ] 验收:构造旧版本场景文件,升级加载后字段正确

### 3.2 序列化重构(渐进,不重写)
- [ ] `ExtractValue` 手写解析包一层类型安全访问器(带错误行号);结构按组件分文件,别全堆 79KB
- [ ] (可选)换 nlohmann::json 正规模板化——`dependencies/json.hpp` 已在项目里,迁移成本低
- [ ] 验收:现有 `sence.json` + 2D 场景往返一致(黄金样本测试已在 Phase 0.3)

### 3.3 编辑器 undo/redo
- [ ] 命令模式:`Transform/组件属性/层级/添加删除实体` 入命令栈(上限 50),Ctrl+Z/Ctrl+Y
- [ ] 先做 Transform + 层级(覆盖 80% 操作),组件属性增量补
- [ ] 验收:连续修改→撤销→重做,场景状态与操作序列一致

### 3.4 (可选)自动保存/崩溃恢复
- [ ] 编辑器每 N 分钟存 `autosave_<scene>.json`,启动时检测并提供恢复

---

## Phase 4 — 迭代回路(2 周)

> 目标:改 shader/贴图不用重启引擎——这是"原型工具"和"引擎"体验的分水岭。

### 4.1 shader 热重载
- [ ] 文件时间戳轮询(500ms)→ 重编译 → 新管线就绪后原子替换,失败保留旧管线 + 日志
- [ ] 接入点:`ShaderManager` + 各 Renderer 持有管线处
- [ ] 验收:编辑 `.frag` 保存,1 秒内画面更新;编译错误时画面不黑(保留旧管线)

### 4.2 GPU profiler
- [ ] `vkCmdWriteTimestamp` + query pool,按 pass(几何/剔除/Hi-Z/后处理/2D)记录 GPU 耗时
- [ ] `ControlPanelWindow`(现只有 CPU FPS,`ControlPanelWindow.cpp:65`)叠加 GPU 逐 pass 条形图
- [ ] 验收:编辑器实时显示逐 pass GPU 毫秒数

### 4.3 资产热重载
- [ ] 贴图/模型文件变更 → 通过 Phase 1 的 AssetManager 重载,编辑器"重新加载"按钮打通

---

## Phase 5 — 质量闭环(1-2 周)

- [ ] 测试覆盖补强:剔除逻辑、chunk 网格一致性、2D 布局(接 Phase 0.3 框架)
- [ ] 崩溃报告增强:`crash_log.txt` 追加 GPU 状态(当前帧号/渲染阶段/最后提交的 pass)
- [ ] 统计面板:draw call、三角面数、CPU/GPU 内存、纹理内存(MRTDebugWindow 扩展)
- [ ] CI 扩展:Android arm64 构建 + 单元测试
- [ ] 验收:每次提交可全量回归;崩溃日志可直接定位到 pass

---

## Phase 6 — 功能面(远期,按游戏需求立项)

> 每项独立评估,不做"标配式"堆功能。建议顺序按第一个目标游戏的需求定。

| 功能 | 工作量 | 依赖 | 备注 |
|---|---|---|---|
| 脚本系统(Lua 先行) | 3-4 周 | Phase 3 undo 稳定后 | 嵌入成本最低;先做组件脚本接口,再考虑 C++ 热更 |
| 动画(骨骼蒙皮 + 状态机) | 4-6 周 | Phase 1 资产系统 | assimp 已能读骨骼数据,缺运行时管线 |
| 后处理栈(Bloom/TAA/SSAO 框架化) | 2-3 周 | Phase 2 多 pass 稳定 | 2D 计划里 bloom 也依赖它(UI 独立 pass) |
| 2D 物理(Box2D) | 1-2 周 | 无 | 2D 计划 Phase 3 已列 |
| 网络 | 4-6 周 | 无 | 立项前先想清楚协议序列化(接 Phase 3) |

---

## 关键决策(为什么这个顺序)

1. **Phase 0 最先**:git + 测试 + 日志是"成本最低、收益最大"的地基;没有它,Phase 1-4 的任何重构都是裸奔
2. **资产系统先于渲染并行**:两者在"上传"上交汇,AssetManager 先定接口,transfer 队列再实现,避免返工
3. **版本化先于 undo**:undo 依赖命令模式,但场景格式不稳定时 undo 存档会做无用功
4. **shader 热重载/GPU profiler 放在架构稳定后**:在单线程渲染上先做热重载,Phase 2 改线程模型时又要重写,不如等
5. **功能面(Phase 6)最后且按需**:没有内容需求驱动的功能开发是浪费——这正是很多个人引擎烂尾的原因

## 与 2D 计划的关系

`2D核心开发计划.md` 的 2D 功能(9-slice、锚点拉伸、补间)可并行推进,不依赖本计划;但 2D 的**离线烘焙工具**和**UI 独立 pass(bloom)**分别依赖 Phase 1(资产打包)和 Phase 6(后处理栈),排期时注意对齐。

"D:\Engine project\vulkan engine\out\build\x64-Release\EngineMain.exe" --project "D:\Engine project\vulkan engine"
